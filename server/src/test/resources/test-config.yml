database:
  url: jdbc:h2:mem:testDb
  driverClass: org.h2.Driver
  user: sa
  password: sa

  properties:
    hibernate.hbm2ddl.auto: create #update #
    hibernate.dialect: org.hibernate.dialect.H2Dialect

logging:
  level: INFO
  loggers:
    nz.co.fit.projectmanagement: DEBUG
